---
title: "flat_additional.Rmd empty"
output: html_document
editor_options: 
  chunk_output_type: console
---

```{r development, include=FALSE}
library(testthat)
```

```{r development-load}
# Load already included functions if relevant
pkgload::load_all(export_all = FALSE)
```

# shiny_alert_isbn_detected_on_img
    
```{r function-shiny_alert_isbn_detected_on_img}
#' @noRd
#' @param detected_isbn_value The detected ISBN value
#' @param ns The namespace
#' @importFrom shinyalert shinyalert
#' @importFrom shiny tags tagAppendChild
#' @rdname fct_mod_120
shiny_alert_isbn_detected_on_img <- function(
  detected_isbn_value,
  ns
) {
  #  voir gestion des ns
  shinyalert(
    html = TRUE,
    title = "ISBN détecté !",
    type = "success",
    showCancelButton = TRUE,
    confirmButtonText = "Ajouter l'ISBN au champ de texte",
    cancelButtonText = "Abandonner",
    callbackJS = sprintf(
      "function(x) {
        //if (x !== false) {
               Shiny.setInputValue('%s', x, {priority: 'event'});
               //}
               }",
      ns("add_detected_isbn_to_textInput")
    ),
    text = tagList(
      tags$div(
        style = "
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;",
        tags$h5(
          sprintf("ISBN détecté : %s", detected_isbn_value)
        )
      )
    )
  )
}
```
  
```{r example-shiny_alert_isbn_detected_on_img}
shiny_alert_isbn_detected_on_img()
```
  
```{r tests-shiny_alert_isbn_detected_on_img}
test_that("shiny_alert_isbn_detected_on_img works", {
  expect_true(inherits(shiny_alert_isbn_detected_on_img, "function"))
})
```
  

# shiny_alert_isbn_not_detected_on_img
    
```{r function-shiny_alert_isbn_not_detected_on_img}
#' @noRd
#' @rdname fct_mod_120
#' @importFrom shinyalert shinyalert
#' @importFrom shiny tags
shiny_alert_isbn_not_detected_on_img <- function() {

}
```
  
```{r example-shiny_alert_isbn_not_detected_on_img}
shiny_alert_isbn_not_detected_on_img()
```
  
```{r tests-shiny_alert_isbn_not_detected_on_img}
test_that("shiny_alert_isbn_not_detected_on_img works", {
  expect_true(inherits(shiny_alert_isbn_not_detected_on_img, "function"))
})
```
  
```{r development-inflate, eval=FALSE}
# Run but keep eval=FALSE to avoid infinite loop
# Execute in the console directly
fusen::inflate(
  flat_file = "dev/flat_mod120.Rmd",
  vignette_name = NA,
  check = FALSE
)
```

