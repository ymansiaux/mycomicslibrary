# WARNING - Generated by {fusen} from dev/flat_mod110.Rmd: do not edit by hand

#' @noRd
#' @param book a cleaned book object
#' @param book_cover book cover
#' @param add_library_button_id id for the add to library button
#' @param add_wishlist_button_id id for the add to wishlist button
#' @param cancel_button_id id for the cancel button
#' @importFrom shinyalert shinyalert
#' @importFrom shiny tags tagAppendChild
#' @rdname fct_mod_110
#' @examples
#' shiny_alert_api_result()
shiny_alert_api_result <- function(
  book,
  book_cover,
  add_library_button_id,
  add_wishlist_button_id,
  cancel_button_id
) {
  shinyalert(
    html = TRUE,
    title = "Livre identifié",
    type = "info",
    showCancelButton = FALSE,
    showConfirmButton = FALSE,
    text = tagList(
      tags$div(
        tags$div(
          style = "
          display: flex;
          flex-direction: column;
          ",
          tags$div(
            tags$ul(
              style = "text-align: left; list-style-type: none;",
              tags$li(
                sprintf("Titre : %s", book$title)
              ),
              tags$li(
                sprintf("Auteur(s) : %s", book$author)
              ),
              tags$li(
                sprintf("Date de publication : %s", book$publish_date)
              ),
              tags$li(
                sprintf("Nombre de pages : %s", book$number_of_pages)
              ),
              tags$li(
                sprintf("Editeur : %s", book$publisher)
              )
            )
          ),
          tags$div(
            tags$img(
              src = book_cover,
              style = "width: 30%; height: auto;"
            )
          )
        ),
        tags$span(
          tags$button(
            id = add_library_button_id,
            class = "btn btn-success",
            tags$span(
              tags$i(class = "fa fa-plus-circle"),
              "Ajouter le livre à ma bibliothèque"
            )
          ),
          tags$button(
            id = add_wishlist_button_id,
            class = "btn btn-info",
            style = "background-color: #469A96; border-color: #356F6D;",
            tags$span(
              tags$i(class = "fa fa-heart"),
              "Ajouter à ma liste d'envies"
            )
          ),
          tags$button(
            id = cancel_button_id,
            class = "btn btn-danger",
            style = "background-color: #d9534f; border-color: #d43f3a;",
            "Abandonner"
          )
        )
      )
    )
  )
}
